# Sync write contract

This is a vendored consumer copy of the canonical write contract in
`woo-rxdb-replication-lab/contracts/write-contract/`.

Until the sync-surface port completes, update the lab contract first and copy
`schema.json` plus `fixtures/*.json` here verbatim. After the port completes,
this directory becomes the single canonical home, per the #478 drift note.
