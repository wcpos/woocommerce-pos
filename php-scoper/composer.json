{
  "name": "wcpos/php-scoper-build",
  "description": "Build-only dependency set that php-scoper prefixes into the plugin's vendor_prefixed directory. Never published, never installed at runtime.",
  "license": "GPL-3.0-or-later",
  "type": "project",
  "require-dev": {
    "humbug/php-scoper": "0.16.2"
  },
  "require": {
    "php": ">=7.4",
    "ext-json": "*",
    "chillerlan/php-qrcode": "^5.0",
    "dompdf/dompdf": "^3.1",
    "firebase/php-jwt": "^6.10.0",
    "picqer/php-barcode-generator": "^2.4"
  },
  "config": {
    "platform": {
      "php": "7.4"
    },
    "preferred-install": "dist",
    "sort-packages": true,
    "audit": {
      "ignore": {
        "PKSA-y2cr-5h3j-g3ys": "Temporary ignore: php-jwt >=6.10.1 requires PHP >=8.0 while plugin minimum remains PHP 7.4."
      }
    }
  }
}
