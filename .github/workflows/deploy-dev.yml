name: Deploy to Dev Site

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    concurrency:
      group: deploy-dev
      cancel-in-progress: false
    runs-on: ubuntu-latest

    steps:
      - name: 🏗 Checkout code
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: 🏗 Setup pnpm
        uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # v4
        with:
          version: '11.1.1'

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: '22'

      - name: 👷 Build
        run: |
          pnpm install --lockfile-only --ignore-scripts
          pnpm install --frozen-lockfile
          composer prefix-dependencies
          composer install --no-dev
          pnpm build

      - name: 📦 Create deployment package
        run: |
          # Create staging directory
          mkdir -p deploy-staging
          
          # Copy files excluding build artifacts and source files
          # NOTE: vendor/ is REQUIRED - it contains PHP classes loaded via composer
          rsync -av \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='node_modules' \
            --exclude='packages/*/node_modules' \
            --exclude='tests' \
            --exclude='.gitignore' \
            --exclude='.editorconfig' \
            --exclude='composer.json' \
            --exclude='composer.lock' \
            --exclude='package.json' \
            --exclude='pnpm-lock.yaml' \
            --exclude='pnpm-workspace.yaml' \
            --exclude='turbo.json' \
            --exclude='tsconfig.json' \
            --exclude='.eslintrc*' \
            --exclude='.prettierrc*' \
            --exclude='*.md' \
            --exclude='packages/*/src' \
            --exclude='packages/*/tsconfig.json' \
            --exclude='packages/*/package.json' \
            ./ deploy-staging/
          
          # Create tarball from staging directory
          cd deploy-staging
          tar -czf ../woocommerce-pos.tar.gz .
          cd ..

      - name: 🔑 Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.HETZNER_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H 213.239.218.130 >> ~/.ssh/known_hosts

      - name: 🚀 Deploy to dev sites
        run: |
          TARBALL="woocommerce-pos-${GITHUB_RUN_ID}.tar.gz"

          # Upload tarball with per-run name to avoid races
          scp -o StrictHostKeyChecking=yes woocommerce-pos.tar.gz "root@213.239.218.130:/tmp/${TARBALL}"

          # Deploy to both dev-free and dev-pro
          ssh -o StrictHostKeyChecking=yes root@213.239.218.130 bash -s -- "${TARBALL}" << 'EOF'
            TARBALL="$1"
            set -e
            FAILED=0

            for SITE in dev-free dev-pro; do
              if (
                set -e
                PLUGIN_DIR="/data/wordpress/${SITE}/html/wp-content/plugins/woocommerce-pos"

                rm -rf "$PLUGIN_DIR"
                mkdir -p "$PLUGIN_DIR"
                tar -xzf "/tmp/${TARBALL}" -C "$PLUGIN_DIR"

                chown -R 82:82 "$PLUGIN_DIR"
              ); then
                echo "✅ Deployed to ${SITE}.wcpos.com"
              else
                echo "❌ Deploy failed for ${SITE}.wcpos.com" >&2
                FAILED=1
                break
              fi
            done

            rm -f "/tmp/${TARBALL}"
            [ "$FAILED" -eq 0 ] || exit 1
          EOF
